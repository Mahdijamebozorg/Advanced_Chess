#ifndef BACKEND_HPP
#define BACKEND_HPP

#include <QDebug>
#include <QObject>
#include <QString>
#include <QtQuick>

class BackEnd : public QObject
{
    Q_OBJECT

private:

public:
    BackEnd();

public slots:

signals:
};

#endif // BACKEND_HPP
